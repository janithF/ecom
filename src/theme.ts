import { extendTheme } from "@chakra-ui/react"

const theme = extendTheme({
  colors: {
    brand: {
      100: "#DFF6FF",
      200: "#3B82F6",
      300: "#1E3A8A",
      400: "#0B212F",
    },
  },
})

export default theme;